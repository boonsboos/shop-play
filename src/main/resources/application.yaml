ktor:
  application:
    modules:
      - nl.connectplay.scoreplay.ApplicationKt.module
  deployment:
    port: 8080
jwt:
  issuer: "https://api.connect-en-play.nl"
  audience: "score-play-users"
  realm: "score-play"
  secret: "unsafeLocalSecret"
cdn:
  base-url: "http://localhost:8082"
  base-path: "/api"

